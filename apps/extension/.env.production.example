# =============================================================================
# EMOTIFYAI EXTENSION - PRODUCTION ENVIRONMENT
# =============================================================================
# IMPORTANT: Replace all placeholder values with actual production credentials
# Copy this file to .env.production for production builds
# NEVER commit .env.production with actual values to version control
# =============================================================================

# -----------------------------------------------------------------------------
# API CONFIGURATION (Production)
# -----------------------------------------------------------------------------
# Base URL for the EmotifyAI backend API (Production)
VITE_API_BASE_URL=https://emotifyai.com/api

# Disable mock API for production
VITE_MOCK_API_ENABLED=false

# -----------------------------------------------------------------------------
# OAUTH CONFIGURATION (Production)
# -----------------------------------------------------------------------------
# OAuth client ID for authentication (Production)
# Get this from Google Cloud Console → APIs & Services → Credentials
# Must match the client ID configured in Supabase Auth settings
VITE_OAUTH_CLIENT_ID=YOUR_PRODUCTION_GOOGLE_OAUTH_CLIENT_ID

# Web application URL for OAuth redirects (Production)
VITE_WEB_APP_URL=https://emotifyai.com

# -----------------------------------------------------------------------------
# EXTENSION CONFIGURATION (Production)
# -----------------------------------------------------------------------------
# Extension ID (Set after publishing to Chrome Web Store / Firefox Add-ons)
# Chrome: Get from chrome://extensions after publishing
# Firefox: Get from about:debugging after publishing
# Leave empty during initial deployment, update after store approval
VITE_EXTENSION_ID=YOUR_PRODUCTION_EXTENSION_ID

# -----------------------------------------------------------------------------
# LOGGING CONFIGURATION (Production)
# -----------------------------------------------------------------------------
# Logging level for production (minimal logging)
# Options: debug, info, warn, error
VITE_LOG_LEVEL=error

# -----------------------------------------------------------------------------
# SECURITY CONFIGURATION (Production)
# -----------------------------------------------------------------------------
# Disable development features
VITE_SKIP_AUTH=false
VITE_DEBUG=false

# -----------------------------------------------------------------------------
# MONITORING (Production)
# -----------------------------------------------------------------------------
# Enable error reporting for production
VITE_ENABLE_ERROR_REPORTING=true

# Sentry DSN for error tracking (Optional but recommended)
VITE_SENTRY_DSN=YOUR_PRODUCTION_SENTRY_DSN

# Enable analytics tracking (Optional)
VITE_ENABLE_ANALYTICS=true

# -----------------------------------------------------------------------------
# FEATURE FLAGS (Production)
# -----------------------------------------------------------------------------
# Disable experimental features in production
VITE_ENABLE_EXPERIMENTAL_FEATURES=false

# -----------------------------------------------------------------------------
# OAUTH REDIRECT CONFIGURATION
# -----------------------------------------------------------------------------
# These URLs must be configured in Google Cloud Console
# Authorized redirect URIs:
# - https://emotifyai.com/auth/callback
# - https://YOUR_SUPABASE_PROJECT_ID.supabase.co/auth/v1/callback
# 
# Authorized JavaScript origins:
# - https://emotifyai.com
# 
# Extension OAuth redirect URL (auto-generated by browser):
# - chrome-extension://YOUR_EXTENSION_ID/
# - moz-extension://YOUR_EXTENSION_ID/

# -----------------------------------------------------------------------------
# STORE SUBMISSION NOTES
# -----------------------------------------------------------------------------
# Chrome Web Store:
# - Update manifest.json version before submission
# - Ensure all permissions are justified in store listing
# - Test extension with production API endpoints
# 
# Firefox Add-ons:
# - Update manifest.json version for Firefox
# - Ensure Manifest V2 compatibility
# - Test cross-browser functionality
# 
# After store approval:
# - Update VITE_EXTENSION_ID with actual extension ID
# - Configure extension ID in backend for security validation
# - Update OAuth settings to include extension redirect URLs
