# =============================================================================
# EMOTIFYAI MONOREPO - ROOT ENVIRONMENT CONFIGURATION
# =============================================================================
# This file serves as a template for root-level environment variables.
# Copy this file to .env and fill in your actual values.
# NEVER commit .env to version control.
# =============================================================================

# -----------------------------------------------------------------------------
# GLOBAL CONFIGURATION
# -----------------------------------------------------------------------------
# These variables are shared across all packages (if needed in future)

# Environment mode: development, test, staging, production
NODE_ENV=development

# -----------------------------------------------------------------------------
# PACKAGE-SPECIFIC CONFIGURATION
# -----------------------------------------------------------------------------
# Each package (extension, web) manages its own environment variables.
# See respective .env.example files in:
# - apps/extension/.env.example
# - apps/web/.env.local.example

# -----------------------------------------------------------------------------
# NOTES
# -----------------------------------------------------------------------------
# 1. Root .env is optional - packages manage their own environments
# 2. No package reads from root .env - each is independent
# 3. This file exists for future shared configuration needs
# 4. For development, configure each package separately:
#    - Extension: apps/extension/.env
#    - Web: apps/web/.env.local
