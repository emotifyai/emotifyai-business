/** Inner panel box for editor input/output text (theme tokens only). */
export const editorTextAreaWrapperClass =
  "relative mt-3 flex min-h-0 flex-1 flex-col rounded-xl border border-border/60 bg-muted/30 p-4"

/** Ghost icon buttons in editor panels — muted hover, not accent/primary (keeps icons visible). */
export const editorToolbarIconButtonClass =
  "h-8 w-8 shrink-0 text-muted-foreground hover:!bg-muted hover:!text-foreground dark:hover:!bg-muted/80"
